@model Booking.Web.Models.NewUserViewModel

@{
    ViewBag.Title = "New User";
}

    <h2>Login</h2>
    <h4>@ViewBag.StatusMessage</h4>

    @using (Html.BeginForm("Index", "Auth", FormMethod.Post))
    {
        @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
        @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
        @Html.PasswordFor(x => x.Password2, new { @class = "form-control" })
        @Html.TextBoxFor(x => x.FirstName, new { @class = "form-control" })
        @Html.TextBoxFor(x => x.LastName, new { @class = "form-control" })
        @Html.TextBoxFor(x => x.Address, new { @class = "form-control" })
        @Html.DropDownListFor(x => x.ZipCode, new SelectList(ViewBag.ZipCodes, "ZipCode", "CityName"))
        @Html.TextBoxFor(x => x.ZipCode, new { @class = "form-control" })
        @Html.TextBoxFor(x => x.PhoneNumber, new { @class = "form-control" })

        <button type="submit" class="btn btn-default">Create</button>
    }

    <p>Hej @ViewBag.Test </p>